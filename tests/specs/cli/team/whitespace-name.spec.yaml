description: team new rejects a whitespace-only name
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: team new "   "
      exit: 1
      stderr: |4+
          
            Error: team name is required

