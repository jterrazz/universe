description: the lockfile rule stays silent when no lockfile exists
fixture: $FIXTURES/docker-pilot/
runs:
    - command: check
      exit: 0
      stdout: |4+
          
            ✓  Project is valid
               {{workdir}}/spwn.yaml

