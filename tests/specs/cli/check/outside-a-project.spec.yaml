description: check outside a spwn project nudges the user at spwn init
fixture: $FIXTURES/empty/
runs:
    - command: check
      exit: 1
      stderr: |4+
          
            Error: no spwn.yaml found — run `spwn init` first

