description: inspecting an agent that was just removed fails without crashing
fixture: $FIXTURES/docker-pilot/
runs:
    - command: agent rm neo
      exit: 0
      stderr: |4+
          
            → Deleting agent "neo"...
            ✓ Deleted agent            neo
            ✓ Updated spwn.yaml        neo

    - command: agent inspect neo
      exit: 1
      stderr: |4+
          
            Error: agent "neo" not found

