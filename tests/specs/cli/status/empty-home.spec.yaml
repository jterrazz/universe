description: status on a home with no worlds renders the empty dashboard
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: status
      exit: 0
      stderr: |4+
          
            spwn vdev
            {{path}} · not configured · 0 skills

            ── Worlds ────────────────────────────────────── none ──
               spwn up --agent <name> -w .

            ── Architect ──────────────────────────────── offline ──

