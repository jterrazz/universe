description: an unknown spwn ref is refused as an unknown builtin
fixture: $FIXTURES/docker-pilot/
runs:
    - command: install spwn:nonesuch
      exit: 1
      stderr: |4+
          
            Error: unknown builtin "spwn:nonesuch" — see the catalog for available dependencies

