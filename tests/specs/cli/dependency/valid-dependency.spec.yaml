description: an spwn:mempalace dependency is accepted without error
fixture: check-valid-dep/
runs:
    - command: check
      exit: 0
      stdout: |4+
          
            ✓  Project is valid
               {{workdir}}/spwn.yaml

