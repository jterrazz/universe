description: a local ref without --agent is refused
fixture: $FIXTURES/docker-pilot/
runs:
    - command: install skill/paper-reading
      exit: 1
      stderr: |4+
          
            Error: "skill/paper-reading" is a local ref — pass --agent <name> to attach it to one agent, or author a new one with `spwn skill new paper-reading` / `spwn/tools/paper-reading/` / `spwn/hooks/paper-reading.yaml` / `spwn/commands/paper-reading.md`

