description: talking to a missing agent suggests creating one
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: agent talk nonexistent hello
      exit: 1
      stderr: |4+
          
            Error: agent "nonexistent" not found

            Create one with: spwn agent create nonexistent

