description: dreaming for a missing agent skips instead of failing
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: agent dream nonexistent
      exit: 0
      stderr: |4+
          
            → Dreaming for agent "nonexistent"...
            Skipped      no journal entries
            Hint         Journal entries are created when worlds are destroyed. Use "spwn down <world>" first.

