description: destroying a missing world names it, hints at world list and dumps no usage
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: down world-nonexistent-00000
      exit: 1
      stderr: |4+
          
            → Destroying world...
            ✗ Destroy failed           world world-nonexistent-00000 not found
              Check that world "world-nonexistent-00000" exists with "spwn world list"

