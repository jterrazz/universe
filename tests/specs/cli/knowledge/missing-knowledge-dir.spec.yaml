description: a missing knowledge directory surfaces a warning, not a crash
fixture: $FIXTURES/knowledge-missing-dir/
runs:
    - command: check
      exit: 0
      stdout: |4+
          
            {{workdir}}/spwn.yaml

            warnings
              •  knowledge path "./knowledge" does not exist for world "neo"
                 spwn.yaml#worlds.neo.knowledge
                 → create the directory (e.g. `mkdir -p ./knowledge`) or drop the `knowledge:` key to disable the bind mount

            0 error(s), 1 warning(s), 0 info

