# Fixture: world declares a knowledge path whose directory is absent
# from disk. Exercises the validator's "path doesn't exist"
# warning surface — recoverable with a `mkdir` but must NOT crash
# or silently bind-mount nothing.
version: 1
name: knowledge-missing-dir

worlds:
  neo:
    agents: [neo]
    workspaces: [.]
    knowledge: ./knowledge
