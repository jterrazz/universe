description: a project mixing a knowledge world with one without passes check
fixture: $FIXTURES/knowledge-mixed-worlds/
runs:
    - command: check
      exit: 0
      stdout: |4+
          
            {{workdir}}/spwn.yaml

            info
              •  world "ephemeral" has no knowledge path; agents will see an empty /world/knowledge/ and won't be told one exists.
                 spwn.yaml#worlds.ephemeral.knowledge
                 → add `knowledge: ./knowledge` (or another path) to enable the shared knowledge base for this world

            0 error(s), 0 warning(s), 1 info

