description: a knowledge path outside the project root is a warning today, not an error
fixture: $FIXTURES/knowledge-outside-root/
runs:
    - command: check
      exit: 0
      stdout: |4+
          
            {{workdir}}/spwn.yaml

            warnings
              •  knowledge path "../escape" does not exist for world "neo"
                 spwn.yaml#worlds.neo.knowledge
                 → create the directory (e.g. `mkdir -p ../escape`) or drop the `knowledge:` key to disable the bind mount

            0 error(s), 1 warning(s), 0 info

