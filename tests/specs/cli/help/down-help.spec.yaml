description: down --help renders the lifecycle usage block
fixture: $FIXTURES/empty/
runs:
    - command: down --help
      exit: 0
      stdout: |+
          
          Destroy a world

          Usage:
            spwn down [name-or-id] [flags]

          Flags:
                --all    Destroy all running worlds
            -h, --help   help for down

