description: --backend on a missing agent fails on the agent, not on the flag
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: agent nonexistent --backend codex
      exit: 1
      stderr: |4+
          
            Error: agent "nonexistent" not found

            Create one with: spwn agent create nonexistent

