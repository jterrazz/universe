description: --world naming a world the manifest does not declare errors
fixture: $FIXTURES/single-agent/
env: isolated
runs:
    - command: logs --world doesnotexist
      exit: 1
      stderr: |4+
          
            Error: unknown world "doesnotexist" — not declared in spwn.yaml

            Known worlds: neo

