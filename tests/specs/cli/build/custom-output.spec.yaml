description: --output writes the tree to a custom location
fixture: $FIXTURES/docker-pilot/
runs:
    - command: build --tree-only --output build/preview
      exit: 0
      stdout: |4+
          
            ✓  Compile complete
               {{workdir}}/build/preview

            2 file(s) written, runtime=claude-code

      files:
          build/preview/agents/neo/CLAUDE.md: exists
          dist: absent
