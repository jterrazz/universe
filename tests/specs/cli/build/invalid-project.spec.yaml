description: build stops at project validation before it touches Docker
fixture: $FIXTURES/check-invalid-tool/
runs:
    - command: build
      exit: 1
      stderr: |4+
          
            Error: project has validation errors - run `spwn check` to see them

