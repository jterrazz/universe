description: --runtime naming an unregistered runtime lists the known ones
fixture: $FIXTURES/docker-pilot/
runs:
    - command: build --tree-only --runtime unknown-runtime
      exit: 1
      stderr: |4+
          
            Error: unknown runtime: unknown-runtime

          Known runtimes: claude-code, codex

