description: build --tree-only writes a tree to ./dist on a minimal project
fixture: $FIXTURES/docker-pilot/
runs:
    - command: build --tree-only
      exit: 0
      stdout: |4+
          
            ✓  Compile complete
               {{workdir}}/dist

            2 file(s) written, runtime=claude-code

      files:
          dist/agents/neo/CLAUDE.md: { contains: "## Role here" }
