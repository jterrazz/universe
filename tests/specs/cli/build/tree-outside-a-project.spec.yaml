description: build --tree-only outside a spwn project nudges the user at spwn init
fixture: $FIXTURES/empty/
runs:
    - command: build --tree-only
      exit: 1
      stderr: |4+
          
            Error: no spwn.yaml found — run `spwn init` first

