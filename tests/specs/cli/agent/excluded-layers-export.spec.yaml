description: export --exclude still writes the archive
fixture: $FIXTURES/empty/
env: isolated
runs:
    - command: agent create neo
      exit: 0
      stderr: |4+
          
            → Creating agent "neo"...
            ✓ Created agent            neo
            ✓ Created soul             SOUL.md

            ✓ Spawn with: spwn up --agent neo

    - command: agent export neo --exclude journal,sessions
      exit: 0
      stderr: |4+
          
            → Exporting agent neo...
            ✓ Exported                 neo.tar.gz

      files:
          neo.tar.gz: exists
