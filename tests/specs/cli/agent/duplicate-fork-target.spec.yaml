description: forking onto an existing target fails cleanly
fixture: $FIXTURES/single-agent/
runs:
    - command: agent fork neo neo-v2
      exit: 0
      stderr: |4+
          
            → Forking "neo" -> "neo-v2"...
            ✓ Source                   neo
            ✓ Target                   neo-v2
            ✓ Layers copied            playbooks, journal
            ✓ Added world to spwn.yaml neo-v2

    - command: agent fork neo neo-v2
      exit: 1
      stderr: |4+
          
            → Forking "neo" -> "neo-v2"...
            ✗ Fork failed              target agent "neo-v2" already exists
              Check agents exist with "spwn agent ls"

