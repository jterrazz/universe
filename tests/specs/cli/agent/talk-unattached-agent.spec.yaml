description: talk to an unattached agent fails cleanly
fixture: $FIXTURES/docker-pilot/
runs:
    - command: agent create orphan
      exit: 0
      stderr: |4+
          
            → Creating agent "orphan"...
            ✓ Created agent            orphan
            ✓ Created soul             SOUL.md
            ✓ Added world to spwn.yaml orphan

            ✓ Spawn with: spwn up --agent orphan

    - command: agent talk orphan hello
      exit: 1
      stderr: |4+
          
            Error: agent "orphan" is not in any active world

            Spawn one with: spwn up --agent orphan -w <workspace>

