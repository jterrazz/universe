description: dream on a missing agent skips gracefully
fixture: $FIXTURES/single-agent/
runs:
    - command: agent dream ghost
      exit: 0
      stderr: |4+
          
            → Dreaming for agent "ghost"...
            Skipped      no journal entries
            Hint         Journal entries are created when worlds are destroyed. Use "spwn down <world>" first.

