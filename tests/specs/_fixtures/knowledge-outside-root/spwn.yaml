# Fixture: world's knowledge path escapes the project root via
# "../escape". Captures today's permissive behaviour — the path
# Surfaces as a "does not exist" warning rather than a hard
# rejection. If the validator ever grows explicit escape detection,
# this fixture's test moves from warning-assert to error-assert.
version: 1
name: knowledge-outside-root

worlds:
  neo:
    agents: [neo]
    workspaces: [.]
    knowledge: ../escape
